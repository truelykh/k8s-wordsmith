apiVersion: v1
kind: Service
metadata:
  namespace: wordsmith
  name: web-service
  labels:
    app: web-service-app
spec:
  selector:
    app: web-pod-deploy-app
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
---
